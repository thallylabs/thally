# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem
.data/

# thally generated artifacts (embeddings cache, analytics)
.thally/
# legacy pre-rebrand cache dir — keep ignored so stale local caches never commit
.dox/
.data/

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

.next
.out
# Workspace package build output — generated by `packages:build` (which the app's
# prebuild runs) and by `prepublishOnly` at npm-publish time, so it needn't be committed.
packages/*/dist

# Local agent/tooling state + scratch — not part of the product, never committed.
.claude/
.agents/
skills-lock.json
CLAUDE.md

# Local demo scaffolds (other projects generated for testing).
demo/

# Personal/process notes. The shared notes/*.md (design-system, product-principles)
# stay tracked; these are local planning + the history-scrub handoff (must never ship).
notes/auth-model.md
notes/thally-v2-plan.md
notes/thally-v2-updated-plan.md
# legacy pre-rebrand names of the two local v2 plan notes — keep ignored
notes/dox-v2-plan.md
notes/dox-v2-updated-plan.md
notes/mintlify-audit.md
notes/worktree-history-scrub-handoff.md
notes/thally-architecture-plan.md
notes/thally-cloud-plan.md

# Local Thally Cloud build-state handoff (mirror of thally-cloud/notes/BUILD-STATE.md)
notes/thally-cloud-BUILD-STATE.md
