name: Manual quality checks

on:
  workflow_dispatch:
    inputs:
      check:
        description: Quality check to run
        required: true
        default: all
        type: choice
        options:
          - all
          - lint
          - core-web-vitals

# These diagnostics only read the checked-out repository. Keep the default
# token unable to write even if a future dependency or build step is compromised.
permissions:
  contents: read

# Manual checks for diagnostics that are useful but too slow or noisy to gate
# every pull request. Select a branch in the Actions UI before starting a run.
jobs:
  lint:
    name: Lint
    if: inputs.check == 'all' || inputs.check == 'lint'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6

      - name: Set up Node
        uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

  lighthouse:
    name: Core Web Vitals budget
    if: inputs.check == 'all' || inputs.check == 'core-web-vitals'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6

      - name: Set up Node
        uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build
        env:
          THALLY_SITE_URL: http://localhost:3040

      - name: Run Lighthouse CI
        run: npx --yes @lhci/cli@0.14.x autorun
